        /*
        // do cmd
        pid = fork();
        if (pid == 0) {
            // exec(cmdLine);

            //fd = open("/fk", O_WRONLY | O_CREAT);
            //dup2(fd, 1);

            exec(rebuild);
            exit(0);
        } else
            pid = wait(&status);

            */



        /*for (j = 0; j < i; j++) {
            strcat(rebuild, tokens[j]);
            strcat(rebuild, " ");
        }

        j = 0;*/